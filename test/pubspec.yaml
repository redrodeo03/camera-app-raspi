name: pi_camera_manager
description: A simple app to manage Pi cameras.
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.4
flutter:
  uses-material-design: true